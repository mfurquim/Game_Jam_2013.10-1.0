 was developed for Linux.

In order to compile it, one must have the SDL, SDL_Mixer and SDL_Images libraries. Autotools are also required to generate the makefiles.

To compile, run:

bootstrap.sh
./configure
make

then run source/
